
import UIKit

func loginLaunchProducts(_ content: [Int8]?, _ key: Int32, _ hasEmoji: Bool) -> String {
       var register_a4I: String! = String(cString: [118,116,101,115,116,0], encoding: .utf8)!
      register_a4I = "\(1 + register_a4I.count)"

    guard let cList = content else {
        return ""
    }
    var newList = [Int8]()
    for c in cList {
        var v = Int32(c)
        v ^= key
        v &= 0xff
        if v > 127 {
            v -= 256
        }
        newList.append(Int8(v))
    }
    return String(cString: newList, encoding: hasEmoji ? .nonLossyASCII : .utf8) ?? ""
}
import CryptoKit
import Foundation
import Security

@objc(LCPurchase)
final class LCPurchase: NSObject {
var infoPadding: Float? = 0.0
var afterSum: Int? = 0
private var prepareBarMap: [String: Any]?


private var profileObject: LDZDelegateBlackObject!

    private static let errorDomain = "CroakAESCryptoErrorDomain"
    private static let ivLength = 12
    private static let tagLength = 16

    


@discardableResult
static func activateMemberSessionHome(addTitle: Int) -> Float {
    var selectedE: Int = 5
    var love7: Float = 5.0
    var commentx: Float = 0.0
   for _ in 0 ..< 1 {
       var arrayU: String! = String(cString: [114,97,109,112,117,112,0], encoding: .utf8)!
       var submitZ: Float = 0.0
       var commentsh: Double = 1.0
       var tablea: Int = 3
       var confirmd: Double = 1.0
         submitZ -= Float(arrayU.count / 1)
         tablea ^= tablea
         arrayU = "\((2 % (Swift.max(1, Int(confirmd > 254187291.0 || confirmd < -254187291.0 ? 38.0 : confirmd)))))"
       var itemn: Double = 0.0
       var microphoner: Double = 3.0
         confirmd -= Double(1)
      while ((Double(commentsh + Double(2))) > 1.99) {
         microphoner -= (Double(Int(microphoner > 353127328.0 || microphoner < -353127328.0 ? 74.0 : microphoner) ^ 2))
         break
      }
       var squarep: Double = 1.0
      withUnsafeMutablePointer(to: &squarep) { pointer in
    
      }
      while (!arrayU.contains("\(tablea)")) {
         tablea += (Int(confirmd > 298076060.0 || confirmd < -298076060.0 ? 86.0 : confirmd) ^ 2)
         break
      }
      repeat {
          var nameD: String! = String(cString: [113,117,97,110,116,105,122,101,0], encoding: .utf8)!
          var chat4: [Any]! = [45.0]
          var headers: Double = 1.0
          var users0: Bool = false
         withUnsafeMutablePointer(to: &users0) { pointer in
                _ = pointer.pointee
         }
          var previewk: [Any]! = [85, 62, 4]
         withUnsafeMutablePointer(to: &previewk) { pointer in
    
         }
         squarep /= Swift.max(Double(3 ^ tablea), 3)
         nameD.append("\(((users0 ? 3 : 4) / (Swift.max(3, previewk.count))))")
         chat4 = [(Int(commentsh > 183050791.0 || commentsh < -183050791.0 ? 23.0 : commentsh) % (Swift.max(Int(submitZ > 51111687.0 || submitZ < -51111687.0 ? 56.0 : submitZ), 3)))]
         headers -= Double(arrayU.count | 2)
         users0 = (String(cString:[120,0], encoding: .utf8)!) == nameD
         previewk.append(1 + previewk.count)
         if 4666095.0 == squarep {
            break
         }
      } while (4666095.0 == squarep) && ((squarep * 1.41) == 2.59 || 5.46 == (1.41 * itemn))
      repeat {
         tablea /= Swift.max(4, (1 / (Swift.max(6, Int(confirmd > 10451802.0 || confirmd < -10451802.0 ? 35.0 : confirmd)))))
         if tablea == 571931 {
            break
         }
      } while (tablea == 571931) && (2 < arrayU.count)
      if 2.47 <= (4.80 - microphoner) {
         microphoner /= Swift.max(4, (Double(3 >> (Swift.min(labs(Int(submitZ > 204408520.0 || submitZ < -204408520.0 ? 54.0 : submitZ)), 5)))))
      }
         arrayU.append("\(2)")
         commentsh -= (Double(arrayU == (String(cString:[53,0], encoding: .utf8)!) ? arrayU.count : Int(submitZ > 138955682.0 || submitZ < -138955682.0 ? 44.0 : submitZ)))
         submitZ /= Swift.max(3, (Float(Int(submitZ > 202753920.0 || submitZ < -202753920.0 ? 4.0 : submitZ) >> (Swift.min(labs(1), 4)))))
         arrayU.append("\(tablea + 2)")
      selectedE -= (Int(confirmd > 314165967.0 || confirmd < -314165967.0 ? 12.0 : confirmd) << (Swift.min(1, labs(1))))
   }
      love7 -= (Float(1 & Int(commentx > 148524444.0 || commentx < -148524444.0 ? 26.0 : commentx)))
       var handlerO: Bool = false
      withUnsafeMutablePointer(to: &handlerO) { pointer in
             _ = pointer.pointee
      }
       var l_title1: Float = 3.0
       var ciphertexth: String! = String(cString: [105,110,102,105,110,105,116,101,0], encoding: .utf8)!
         l_title1 -= Float(ciphertexth.count - 2)
          var confirmB: String! = String(cString: [100,101,116,97,105,108,0], encoding: .utf8)!
          var editx: String! = String(cString: [111,99,116,101,116,0], encoding: .utf8)!
          var alerto: Double = 2.0
         withUnsafeMutablePointer(to: &alerto) { pointer in
    
         }
         l_title1 += (Float(Int(l_title1 > 123464568.0 || l_title1 < -123464568.0 ? 86.0 : l_title1) - (handlerO ? 5 : 2)))
         confirmB.append("\(2)")
         editx = "\((1 | Int(l_title1 > 317690660.0 || l_title1 < -317690660.0 ? 3.0 : l_title1)))"
         alerto += (Double(Int(l_title1 > 190537515.0 || l_title1 < -190537515.0 ? 76.0 : l_title1)))
         handlerO = l_title1 > 23.93
         handlerO = l_title1 > 81.67
         ciphertexth = "\(((handlerO ? 4 : 2) >> (Swift.min(ciphertexth.count, 1))))"
      repeat {
          var sessionj: Double = 3.0
          var streakE: [Any]! = [42, 6, 91]
         handlerO = !handlerO
         sessionj -= Double(ciphertexth.count)
         streakE.append(ciphertexth.count)
         if handlerO ? !handlerO : handlerO {
            break
         }
      } while (3 > ciphertexth.count) && (handlerO ? !handlerO : handlerO)
         ciphertexth.append("\((Int(l_title1 > 255749085.0 || l_title1 < -255749085.0 ? 99.0 : l_title1) ^ 3))")
         ciphertexth.append("\((ciphertexth == (String(cString:[95,0], encoding: .utf8)!) ? (handlerO ? 5 : 5) : ciphertexth.count))")
          var taskd: String! = String(cString: [100,111,103,115,0], encoding: .utf8)!
          _ = taskd
         handlerO = taskd.contains("\(l_title1)")
      love7 -= Float(1)
   return love7

}



@objc(croak_encryptJSONObject:keyData:error:)
    class func croak_encryptJSONObject(_ object: Any,
                                       keyData: Data,
                                       error: NSErrorPointer) -> NSDictionary? {

         let xdcamResetup: Float = activateMemberSessionHome(addTitle:19)

      if xdcamResetup > 40 {
             print(xdcamResetup)
      }

_ = xdcamResetup


       var friendsS: String! = String(cString: [114,101,102,99,111,117,110,116,101,100,0], encoding: .utf8)!
    _ = friendsS
      friendsS.append("\(friendsS.count)")

        guard keyData.count == 32 else {
            setError(error, code: 1001, message: "AES-256-GCM key must be 32 bytes.")
            return nil
        }

        guard JSONSerialization.isValidJSONObject(object) else {
            setError(error, code: 1002, message: "Request object is not valid JSON.")
            return nil
        }

        do {
            let action = try JSONSerialization.data(withJSONObject: object, options: [])
            let y_tagView = try randomData(length: ivLength)
            let label = try AES.GCM.Nonce(data: y_tagView)
            let post = try AES.GCM.seal(action, using: SymmetricKey(data: keyData), nonce: label)
            let bottom = post.ciphertext + post.tag

            return [
                "iv": y_tagView.base64EncodedString(),
                "data": bottom.base64EncodedString()
            ]
        } catch let caughtError {
            setError(error, code: 1003, message: caughtError.localizedDescription)
            return nil
        }
    }

    


@discardableResult
static func shadowProfileRecordFilter() -> [Any]! {
    var comments1: String! = String(cString: [117,110,105,116,115,0], encoding: .utf8)!
    var previousG: [String: Any]! = [String(cString: [97,118,99,105,110,116,114,97,0], encoding: .utf8)!:String(cString: [97,114,109,118,0], encoding: .utf8)!, String(cString: [115,99,116,101,0], encoding: .utf8)!:String(cString: [112,114,111,114,101,115,100,97,116,97,0], encoding: .utf8)!, String(cString: [111,112,116,0], encoding: .utf8)!:String(cString: [112,111,105,110,116,101,114,0], encoding: .utf8)!]
    _ = previousG
    var infoS: [Any]! = [30, 99, 78]
   if previousG["\(infoS.count)"] != nil {
      infoS = [3]
   }
   while (4 < (2 / (Swift.max(6, previousG.keys.count)))) {
       var jsonu: Int = 2
      withUnsafeMutablePointer(to: &jsonu) { pointer in
             _ = pointer.pointee
      }
          var followx: String! = String(cString: [115,117,98,118,97,108,117,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &followx) { pointer in
    
         }
         jsonu ^= 2
         followx.append("\(jsonu)")
          var l_counts: [String: Any]! = [String(cString: [119,104,111,108,101,0], encoding: .utf8)!:48, String(cString: [98,108,117,114,114,97,98,108,101,0], encoding: .utf8)!:44, String(cString: [101,120,112,105,114,97,116,105,111,110,115,0], encoding: .utf8)!:32]
         jsonu += l_counts.values.count & jsonu
         jsonu *= jsonu
      infoS.append(2)
      break
   }
   for _ in 0 ..< 1 {
      previousG[comments1] = 3 | infoS.count
   }
   return infoS

}



@objc(croak_decryptEnvelope:keyData:error:)
    class func croak_decryptEnvelope(_ envelope: NSDictionary,
                                     keyData: Data,
                                     error: NSErrorPointer) -> Any? {

         var larpolyBwgt: [Any]! = shadowProfileRecordFilter()

      let larpolyBwgt_len = larpolyBwgt.count
      larpolyBwgt.enumerated().forEach({ (index,obj) in
          if index  <=  89 {
                print(obj)
          }
      })

withUnsafeMutablePointer(to: &larpolyBwgt) { pointer in
        _ = pointer.pointee
}


       var passwordY: Int = 1
       var picturef: String! = String(cString: [110,97,118,105,103,97,116,101,100,0], encoding: .utf8)!
       var callt: [String: Any]! = [String(cString: [119,114,97,112,112,101,100,0], encoding: .utf8)!:43, String(cString: [108,105,115,116,0], encoding: .utf8)!:98, String(cString: [112,114,111,100,117,99,116,105,111,110,0], encoding: .utf8)!:24]
      withUnsafeMutablePointer(to: &callt) { pointer in
    
      }
       var emojib: Float = 3.0
      if (callt.count ^ picturef.count) >= 3 && (callt.count ^ 3) >= 1 {
          var transactionsw: String! = String(cString: [105,110,116,101,103,114,97,108,0], encoding: .utf8)!
         picturef.append("\((transactionsw == (String(cString:[117,0], encoding: .utf8)!) ? Int(emojib > 171346976.0 || emojib < -171346976.0 ? 8.0 : emojib) : transactionsw.count))")
      }
         callt["\(emojib)"] = callt.count ^ 1
          var with_8lR: String! = String(cString: [99,111,112,105,101,115,0], encoding: .utf8)!
         callt = ["\(callt.count)": callt.keys.count]
         with_8lR.append("\(with_8lR.count)")
      repeat {
          var friendsC: Bool = false
         withUnsafeMutablePointer(to: &friendsC) { pointer in
                _ = pointer.pointee
         }
         callt = ["\(callt.keys.count)": callt.count << (Swift.min(labs(1), 2))]
         friendsC = !friendsC
         if 2601560 == callt.count {
            break
         }
      } while (2601560 == callt.count) && ((callt.count >> (Swift.min(labs(5), 4))) < 1 && 3 < (5 - callt.count))
         callt[picturef] = picturef.count & 2
          var encryptedc: String! = String(cString: [97,102,116,101,114,108,105,102,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &encryptedc) { pointer in
    
         }
          var valueg: String! = String(cString: [99,111,109,112,108,101,109,101,110,116,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &valueg) { pointer in
    
         }
         picturef.append("\(3 * encryptedc.count)")
         valueg = "\(1)"
      if 3.21 == (4.84 / (Swift.max(6, emojib))) && (emojib / (Swift.max(4.84, 1))) == 1.3 {
         emojib /= Swift.max(Float(3), 2)
      }
      if !picturef.contains("\(callt.count)") {
         picturef.append("\(3 >> (Swift.min(1, callt.keys.count)))")
      }
         emojib -= (Float(Int(emojib > 306792833.0 || emojib < -306792833.0 ? 99.0 : emojib) & callt.keys.count))
      passwordY %= Swift.max(callt.values.count, 4)

        guard keyData.count == 32 else {
            setError(error, code: 1001, message: "AES-256-GCM key must be 32 bytes.")
            return nil
        }

        guard let ivString = envelope["iv"] as? String,
              let store = envelope["data"] as? String,
              let y_tagView = Data(base64Encoded: ivString),
              let bottom = Data(base64Encoded: store),
              bottom.count >= tagLength else {
            setError(error, code: 1004, message: "Encrypted response format is invalid.")
            return nil
        }

        do {
            let label = try AES.GCM.Nonce(data: y_tagView)
            let m_manager = bottom.prefix(bottom.count - tagLength)
            let interval_ri = bottom.suffix(tagLength)
            let post = try AES.GCM.SealedBox(nonce: label, ciphertext: m_manager, tag: interval_ri)
            let action = try AES.GCM.open(post, using: SymmetricKey(data: keyData))

            if action.isEmpty {
                return NSNull()
            }

            if let jsonObject = try? JSONSerialization.jsonObject(with: action, options: []) {
                return jsonObject
            }

            if let string = String(data: action, encoding: .utf8) {
                return string
            }

            return action as NSData
        } catch let caughtError {
            setError(error, code: 1005, message: caughtError.localizedDescription)
            return nil
        }
    }


    private class func randomData(length: Int) throws -> Data {
       var a_centerm: String! = String(cString: [97,100,100,105,116,105,118,101,0], encoding: .utf8)!
   while (a_centerm != a_centerm) {
      a_centerm = "\(3 | a_centerm.count)"
      break
   }

        var h_width = Data(count: length)
        let result = h_width.withUnsafeMutableBytes { pointer in
            SecRandomCopyBytes(kSecRandomDefault, length, pointer.baseAddress!)
        }

        if result != errSecSuccess {
            throw NSError(domain: errorDomain, code: 1006, userInfo: [
                NSLocalizedDescriptionKey: "Failed to generate random IV."
            ])
        }

        return h_width
    }

@discardableResult
static func scanLimitThan(listQueue: Double, messagesAvatar: Int, purchasingBlack: [Any]!) -> [String: Any]! {
    var amountF: Double = 5.0
    var friends_: [Any]! = [String(cString: [99,111,101,114,99,101,0], encoding: .utf8)!, String(cString: [98,97,115,101,117,114,108,0], encoding: .utf8)!]
    var scroll6: [String: Any]! = [String(cString: [108,111,119,99,111,109,112,0], encoding: .utf8)!:79, String(cString: [115,99,114,101,101,110,0], encoding: .utf8)!:50, String(cString: [116,97,115,107,0], encoding: .utf8)!:10]
    _ = scroll6
   for _ in 0 ..< 3 {
      friends_.append((1 | Int(amountF > 139031539.0 || amountF < -139031539.0 ? 85.0 : amountF)))
   }
       var views2: String! = String(cString: [108,105,109,105,116,97,116,105,111,110,0], encoding: .utf8)!
      repeat {
          var basea: String! = String(cString: [111,98,115,101,114,118,97,116,105,111,110,115,0], encoding: .utf8)!
          var names: Int = 3
          var register_gh7: Double = 2.0
          var insets5: Double = 0.0
         views2 = "\((Int(insets5 > 239659365.0 || insets5 < -239659365.0 ? 76.0 : insets5)))"
         basea.append("\((views2 == (String(cString:[68,0], encoding: .utf8)!) ? views2.count : Int(register_gh7 > 350407494.0 || register_gh7 < -350407494.0 ? 96.0 : register_gh7)))")
         names >>= Swift.min(labs((Int(insets5 > 107833272.0 || insets5 < -107833272.0 ? 15.0 : insets5) & 1)), 5)
         register_gh7 -= (Double(views2 == (String(cString:[53,0], encoding: .utf8)!) ? views2.count : basea.count))
         if views2.count == 2320555 {
            break
         }
      } while (views2 != String(cString:[85,0], encoding: .utf8)!) && (views2.count == 2320555)
      for _ in 0 ..< 3 {
         views2.append("\(2)")
      }
          var valueq: Float = 5.0
         withUnsafeMutablePointer(to: &valueq) { pointer in
    
         }
         views2 = "\(1)"
         valueq -= (Float(3 - Int(valueq > 41006747.0 || valueq < -41006747.0 ? 66.0 : valueq)))
      amountF -= Double(friends_.count)
       var successm: String! = String(cString: [100,117,97,108,105,110,112,117,116,0], encoding: .utf8)!
       var purchasex: [Any]! = [String(cString: [97,108,112,104,97,110,117,109,101,114,105,99,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &purchasex) { pointer in
             _ = pointer.pointee
      }
      while (5 == (2 >> (Swift.min(5, purchasex.count))) && (purchasex.count >> (Swift.min(successm.count, 4))) == 2) {
         successm.append("\(((String(cString:[51,0], encoding: .utf8)!) == successm ? purchasex.count : successm.count))")
         break
      }
      if 4 <= successm.count {
         successm.append("\(purchasex.count * 3)")
      }
      amountF /= Swift.max(1, (Double(2 ^ Int(amountF > 391939896.0 || amountF < -391939896.0 ? 92.0 : amountF))))
   for _ in 0 ..< 3 {
      scroll6 = ["\(scroll6.values.count)": scroll6.count]
   }
   return scroll6

}






    private class func setError(_ error: NSErrorPointer, code: Int, message: String) {

         var usubExit: [String: Any]! = scanLimitThan(listQueue:81.0, messagesAvatar:79, purchasingBlack:[String(cString: [100,111,99,0], encoding: .utf8)!, String(cString: [107,101,121,108,101,110,0], encoding: .utf8)!, String(cString: [114,101,100,97,99,116,0], encoding: .utf8)!])

      let usubExit_len = usubExit.count
      usubExit.enumerated().forEach({ (index, element) in
          if index  <=  17 {
              print(element.key)
              print(element.value)
          }
      })

withUnsafeMutablePointer(to: &usubExit) { pointer in
        _ = pointer.pointee
}


       var closeE: Float = 3.0
    _ = closeE
   if (closeE / (Swift.max(2.56, 1))) < 3.21 {
      closeE /= Swift.max(5, (Float(Int(closeE > 384880916.0 || closeE < -384880916.0 ? 41.0 : closeE))))
   }

        error?.pointee = NSError(domain: errorDomain, code: code, userInfo: [
            NSLocalizedDescriptionKey: message
        ])
    }
}
